---
// Remove current url path and remove trailing slash if exists
const rawPath = Astro.url.pathname.replace(/\/\/+$/, "");

// Base URL as configured (remove trailing slash)
const baseUrl = (import.meta.env.BASE_URL ?? "/").replace(/\/\/+$/, "");

// Build path segments and strip base segments when site is served under a subpath
// e.g. rawPath: '/blog/garden' => pathSegments ['blog','garden'], baseSegments ['blog'] => breadcrumbList ['garden']
const pathSegments = rawPath.split("/").filter(Boolean);
const baseSegments = baseUrl === "/" ? [] : baseUrl.split("/").filter(Boolean);

let breadcrumbList = pathSegments;
if (
  baseSegments.length &&
  pathSegments.slice(0, baseSegments.length).join("/") ===
    baseSegments.join("/")
) {
  breadcrumbList = pathSegments.slice(baseSegments.length);
}

// if breadcrumb is Home > Posts > 1 <etc>
// replace Posts with Posts (page number)
if (breadcrumbList[0] === "posts") {
  breadcrumbList.splice(0, 2, `Posts (page ${breadcrumbList[1] || 1})`);
}

// if breadcrumb is Home > Tags > [tag] > [page] <etc>
// replace [tag] > [page] with [tag] (page number)
if (breadcrumbList[0] === "tags" && !isNaN(Number(breadcrumbList[2]))) {
  breadcrumbList.splice(
    1,
    3,
    `${breadcrumbList[1]} ${Number(breadcrumbList[2]) === 1 ? "" : "(page " + breadcrumbList[2] + ")"}`
  );
}
---

<nav class="mx-auto mt-8 mb-1 w-full max-w-app px-4" aria-label="breadcrumb">
  <ul
    class="font-light [&>li]:inline [&>li:not(:last-child)>a]:hover:opacity-100"
  >
    <li>
      <a href={`${import.meta.env.BASE_URL ?? "/"}`} class="opacity-80">Home</a>
      <span aria-hidden="true" class="opacity-80">&raquo;</span>
    </li>
    {
      breadcrumbList.map((breadcrumb, index) =>
        index + 1 === breadcrumbList.length ? (
          <li>
            <span
              class:list={["capitalize opacity-75", { lowercase: index > 0 }]}
              aria-current="page"
            >
              {/* make the last part lowercase in Home > Tags > some-tag */}
              {decodeURIComponent(breadcrumb)}
            </span>
          </li>
        ) : (
          <li>
            <a
              href={`${import.meta.env.BASE_URL ?? "/"}${breadcrumb}/`}
              class="capitalize opacity-70"
            >
              {breadcrumb}
            </a>
            <span aria-hidden="true">&raquo;</span>
          </li>
        )
      )
    }
  </ul>
</nav>
